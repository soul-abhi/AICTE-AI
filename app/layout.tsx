import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Toaster } from "react-hot-toast";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "AI Resume Builder - Build 90+ ATS Score Resumes",
  description: "Generate professional, ATS-optimized resume summaries using AI. Build your perfect resume in minutes.",
  keywords: "AI resume builder, ATS resume, resume generator, professional resume, AI-powered resume",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster position="top-right" />
      </body>
    </html>
  );
}
